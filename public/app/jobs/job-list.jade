.container
  table.table.table-hover.table-striped.table-condensed
    thead
      tr
        th Company
        th Job Title
        th Job Description
        th Date
      tbody
        tr(ng-repeat="job in jobs")
          td {{ job.company }}
          td {{ job.title }}
          td {{ job.description }}
          td {{ job.date | date }}
